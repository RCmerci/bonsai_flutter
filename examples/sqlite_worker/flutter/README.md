# SQLite Worker Todo Flutter host

This directory contains the thin Flutter host for the OCaml-owned SQLite
Worker Todo example. See the [example README](../README.md) for architecture,
build commands, storage behavior, and support boundaries.
