# Todo Flutter Host

This directory is the native Flutter shell for the OCaml-owned Todo example.
Build and run commands, including the unsigned iPhoneOS command and current
support boundary, are documented in `../README.md`.
